# Local development can run without DATABASE_URL and use data/*.json.
# Production MUST set DATABASE_URL to a Supabase/PostgreSQL connection string.
DATABASE_URL=
DATABASE_SSL=require
DB_POOL_MAX=5
NODE_ENV=production
# Set this once in Render. Do not commit the real value.
ADMIN_SETUP_KEY=
